name: calai
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.8.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_markdown: ^0.6.18

  flutter_gemini: ^3.0.0
  google_generative_ai: ^0.4.7

  # All packages below have been moved to the correct indentation level.
  cupertino_icons: ^1.0.8
  vector_math: ^2.1.4

  # Firebase dependencies
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  cloud_firestore: ^6.0.1
  firebase_storage: ^13.0.1
  firebase_analytics: ^12.0.1
  image_picker: ^1.0.7
  cloudinary_public: 0.23.1
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/login_hii.jpg
    - assets/images/login_vector.png
    - assets/images/register_vector.png
